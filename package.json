{
  "name": "panthora",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "turbo start",
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "dependency-update": "npm-check-updates -u --filterVersion \"/^[~^<>]| - |\\.x$/\" --deep && pnpm install"
  },
  "engines": {
    "node": "20",
    "pnpm": "8"
  },
  "packageManager": "pnpm@8.7.6",
  "devDependencies": {
    "turbo": "^1.12.5"
  }
}
